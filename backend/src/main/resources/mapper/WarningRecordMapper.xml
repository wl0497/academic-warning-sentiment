<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.warning.mapper.WarningRecordMapper">
    <select id="countByLevel" resultType="map">
        SELECT warning_level, COUNT(*) as count FROM warning_record GROUP BY warning_level
    </select>
    <select id="countByHandled" resultType="map">
        SELECT handled, COUNT(*) as count FROM warning_record GROUP BY handled
    </select>
</mapper>
